
  #git commands#
  creates a new brach / switch into one:
  git checkout -b my-new-branch 

  git add .
git commit -m "add changes"

git push -u origin my-new-branch

or 


git push origin main:button
